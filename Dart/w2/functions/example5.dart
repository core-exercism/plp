// Anonymous function

// Main function that prints each element of the fruits list.
void main() {
  const fruits = [
    'apple',
    'banana',
    'orange',
  ];

  fruits.forEach((element) { 
    print(element);
  });
}